<Window x:Class="ListaryOpen.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ListaryOpen Settings"
        Icon="{StaticResource ListaryOpenIcon}"
        Height="360"
        Width="520"
        MinHeight="300"
        MinWidth="420"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="ListaryOpen Settings"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Margin="0,0,0,6" />

        <TextBlock Grid.Row="1"
                   Text="{Binding IndexingStatusText}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,16" />

        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Indexed roots"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Margin="0,0,0,6" />
                <Border BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                        BorderThickness="1"
                        Padding="10"
                        Margin="0,0,0,16">
                    <ItemsControl ItemsSource="{Binding Settings.IndexedRoots}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"
                                           TextWrapping="Wrap"
                                           Margin="0,2" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>

                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0"
                                Margin="0,0,10,0">
                        <TextBlock Text="Search hotkey"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,6" />
                        <TextBlock Text="{Binding Settings.SearchHotkey}"
                                   FontSize="16"
                                   TextWrapping="Wrap" />
                    </StackPanel>

                    <StackPanel Grid.Column="1"
                                Margin="10,0,0,0">
                        <TextBlock Text="Dialog hotkey"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,6" />
                        <TextBlock Text="{Binding Settings.DialogHotkey}"
                                   FontSize="16"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </Grid>

                <CheckBox Content="Quick Save / Open enabled"
                          IsChecked="{Binding Settings.QuickSaveOpenEnabled, Mode=OneWay}"
                          IsEnabled="False"
                          Margin="0,0,0,16" />

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0"
                                Margin="0,0,12,0">
                        <TextBlock Text="NTFS fast indexing"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,6" />
                        <TextBlock Text="{Binding NtfsFastIndexingStatusText}"
                                   TextWrapping="Wrap" />
                    </StackPanel>

                    <Button Grid.Column="1"
                            MinWidth="80"
                            Padding="10,4"
                            VerticalAlignment="Center"
                            Content="Enable"
                            Command="{Binding EnableNtfsFastIndexingCommand}" />
                </Grid>

                <Grid Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0"
                                Margin="0,0,12,0">
                        <TextBlock Text="Hook quick switch"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,6" />
                        <TextBlock Text="{Binding HookQuickSwitchStatusText}"
                                   TextWrapping="Wrap" />
                    </StackPanel>

                    <Button Grid.Column="1"
                            MinWidth="80"
                            Padding="10,4"
                            VerticalAlignment="Center"
                            Content="Enable"
                            Command="{Binding EnableHookQuickSwitchCommand}" />
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
