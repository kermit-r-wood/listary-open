<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="SettingsSectionStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource Brush.Surface}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{StaticResource Radius.Section}" />
        <Setter Property="Padding" Value="{StaticResource Thickness.SectionPadding}" />
        <Setter Property="Margin" Value="0,0,0,12" />
    </Style>

    <Style x:Key="SettingsRowTitleStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource Font.Primary}" />
        <Setter Property="FontSize" Value="{StaticResource FontSize.Body}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextPrimary}" />
        <Setter Property="LineHeight" Value="20" />
        <Setter Property="LineStackingStrategy" Value="BlockLineHeight" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style x:Key="SettingsDetailStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource Font.Primary}" />
        <Setter Property="FontSize" Value="{StaticResource FontSize.Small}" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextMuted}" />
        <Setter Property="LineHeight" Value="18" />
        <Setter Property="LineStackingStrategy" Value="BlockLineHeight" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style x:Key="SettingsStatusBadgeStyle" TargetType="Border">
        <Setter Property="MinWidth" Value="72" />
        <Setter Property="Padding" Value="10,4" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border}" />
        <Setter Property="Background" Value="{DynamicResource Brush.SurfaceMuted}" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Style.Triggers>
            <Trigger Property="Tag" Value="Enabled">
                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Success}" />
                <Setter Property="Background" Value="{DynamicResource Brush.SuccessSoft}" />
            </Trigger>
            <Trigger Property="Tag" Value="Ready">
                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Success}" />
                <Setter Property="Background" Value="{DynamicResource Brush.SuccessSoft}" />
            </Trigger>
            <Trigger Property="Tag" Value="Degraded">
                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Warning}" />
                <Setter Property="Background" Value="{DynamicResource Brush.WarningSoft}" />
            </Trigger>
            <Trigger Property="Tag" Value="Failed">
                <Setter Property="BorderBrush" Value="{DynamicResource Brush.Error}" />
                <Setter Property="Background" Value="{DynamicResource Brush.ErrorSoft}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SettingsStatusBadgeTextStyle" TargetType="TextBlock">
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="{StaticResource Font.Primary}" />
        <Setter Property="FontSize" Value="{StaticResource FontSize.Small}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextSecondary}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Border}, Path=Tag}" Value="Enabled">
                <Setter Property="Foreground" Value="{DynamicResource Brush.Success}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Border}, Path=Tag}" Value="Ready">
                <Setter Property="Foreground" Value="{DynamicResource Brush.Success}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Border}, Path=Tag}" Value="Degraded">
                <Setter Property="Foreground" Value="{DynamicResource Brush.Warning}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Border}, Path=Tag}" Value="Failed">
                <Setter Property="Foreground" Value="{DynamicResource Brush.Error}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SettingsPathTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Cascadia Mono, Consolas" />
        <Setter Property="FontSize" Value="{StaticResource FontSize.Small}" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.TextSecondary}" />
        <Setter Property="LineHeight" Value="18" />
        <Setter Property="LineStackingStrategy" Value="BlockLineHeight" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        <Setter Property="TextWrapping" Value="NoWrap" />
        <Setter Property="Margin" Value="0,2,0,2" />
    </Style>
</ResourceDictionary>
